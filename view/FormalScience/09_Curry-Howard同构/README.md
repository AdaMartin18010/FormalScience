# 09_Curry-Howard同构

> **主题**: 逻辑、类型与程序的统一对应关系
> **来源**: view08.md

## 📋 子主题

### 09.1_逻辑与类型的对应

- 命题即类型
- 证明即程序
- 逻辑连接词与类型构造子

### 09.2_基本对应关系

- ∧ (与) ↔ 积类型 (A × B) ↔ Tuple
- ∨ (或) ↔ 余积类型 (A + B) ↔ Either
- ⇒ (蕴含) ↔ 指数类型 (A ⇒ B) ↔ Function
- ∀ ↔ Π-类型 ↔ Dependent Function
- ∃ ↔ Σ-类型 ↔ Dependent Pair

### 09.3_范畴论视角

- 逻辑范畴
- 伴随立方
- 自然变换
- 交换图验证

### 09.4_依赖类型系统

- 依赖类型定义
- 类型族
- 纤维化范畴
- 拉回替换

### 09.5_实际应用

- 定理证明
- 程序验证
- 类型安全
- 形式化验证

## 📚 核心内容

### Curry-Howard对应表

| 逻辑 | 范畴 | 编程语言 | 证明 |
|------|------|----------|------|
| ∧ (与) | 积 | Tuple (A, B) | 对构造 |
| ∨ (或) | 余积 | Either A B | 情况分析 |
| ⇒ (蕴含) | 指数 | Function A -> B | λ抽象 |
| ∀ | Π-对象 | Dep Function | 泛证 |
| ∃ | Σ-对象 | Dep Pair | 见证提取 |

### 基本对应关系详解

#### 1. ∧ (与) ↔ 积类型 (A × B) ↔ Tuple

**逻辑侧**: 命题 `A ∧ B` 表示"A与B同时为真"

**编程侧**:

```haskell
-- 构造子（introduction）
pair :: A -> B -> (A, B)
pair a b = (a, b)

-- 消去子（elimination）
fst :: (A, B) -> A
snd :: (A, B) -> B
```

**范畴侧**: 积 A × B 满足泛性质

#### 2. ∨ (或) ↔ 余积类型 (A + B) ↔ Either

**逻辑侧**: 命题 `A ∨ B` 表示"A或B至少一个为真"

**编程侧**:

```haskell
data Either a b = Left a | Right b

-- 消去子（模式匹配 = 证明分情况）
caseEither :: Either A B -> (A -> C) -> (B -> C) -> C
caseEither (Left a)  f g = f a
caseEither (Right b) f g = g b
```

#### 3. ⇒ (蕴含) ↔ 指数类型 (A ⇒ B) ↔ Function

**逻辑侧**: 命题 `A ⇒ B` 表示"若A为真，则B为真"

**编程侧**:

```haskell
-- λ抽象 = 蕴含引入
lambda :: (A -> B) -> (A -> B)
lambda f = f

-- 函数应用 = 蕴含消去（Modus Ponens）
apply :: (A -> B) -> A -> B
apply f a = f a
```

#### 4. ∀ ↔ Π-类型 ↔ Dependent Function

**逻辑侧**: 命题 `∀x:A. P(x)` 表示"对所有A中的x，P(x)为真"

**编程侧**:

```idris
-- 依赖类型：向量的长度依赖其元素类型
Vec : (n : Nat) -> (a : Type) -> Type

-- Π-类型：证明∀n:Nat. Vec n A → Vec n A
reverse : (n : Nat) -> Vec n a -> Vec n a
```

#### 5. ∃ ↔ Σ-类型 ↔ Dependent Pair

**逻辑侧**: 命题 `∃x:A. P(x)` 表示"存在A中的x，使P(x)为真"

**编程侧**:

```idris
-- Σ-类型：证明"存在非空向量"
data ExistsNonEmpty : (a : Type) -> Type where
  MkNonEmpty : (n : Nat) -> (vec : Vec (S n) a) -> ExistsNonEmpty a
```

### 范畴论视角

#### 伴随立方

```text
         Π
        / \
       /   \
      /     \
   ⇒        ×
    \     /
     \   /
      ∨ +
       \ /
         Σ
```

- **右伴随**: ×, Π, ⇒
- **左伴随**: +, Σ
- **双重否定延续（CNF）**: Curry-Howard-Lambek三重同构

### 实际应用

#### 1. 定理证明

- 用程序证明数学定理
- 类型检查即证明验证
- 构造性证明

#### 2. 程序验证

- 类型安全保证
- 形式化验证
- 正确性证明

#### 3. 依赖类型语言

- Idris
- Agda
- Lean
- Coq

## 🔗 相关文档

- [view08.md](../../view08.md) - Curry-Howard同构完整论述

## 📖 扩展阅读

- Wikipedia: [Curry-Howard Correspondence](https://en.wikipedia.org/wiki/Curry%E2%80%93Howard_correspondence)
- 《Types and Programming Languages》
- 《Proofs and Types》
- 《Homotopy Type Theory》
