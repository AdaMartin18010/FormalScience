- --
topic: "2.2 Text Format"
dependencies: []
status: "review"
author: "FormalScience Project"
date: "2026-04-12"
version: "1.0.0"
tags: ["递归", "类型", "定理", "集合", "模型"]
category: "reference"
priority: "medium"
- --

# 2.2 Text Format

> **子主题编号**: 02.2
> **主题**: Wasm视角

## 1. 目录 {#目录}

- [2.2 Text Format](#22-text-format)
  - [1. 目录 {#目录}](#1-目录-目录)
  - [2. 核心命题 {#核心命题}](#2-核心命题-核心命题)
  - [1. S-表达式语法 {#s-表达式语法}](#1-s-表达式语法-s-表达式语法)
    - [1 基本结构 {#基本结构}](#1-基本结构-基本结构)
    - [1 2 折叠指令（Folded Instructions） {#2-折叠指令folded-instructions}](#1-2-折叠指令folded-instructions-2-折叠指令folded-instructions)
  - [2. 模块声明 {#模块声明}](#2-模块声明-模块声明)
    - [1 Type Section {#type-section}](#1-type-section-type-section)
    - [2 2 Function 声明 {#2-function-声明}](#2-2-function-声明-2-function-声明)
  - [3. 控制流结构 {#控制流结构}](#3-控制流结构-控制流结构)
    - [1 Block 与 Label {#block-与-label}](#1-block-与-label-block-与-label)
    - [3 2 Loop 结构 {#2-loop-结构}](#3-2-loop-结构-2-loop-结构)
    - [3 3 If-Else {#3-if-else}](#3-3-if-else-3-if-else)
  - [4. 内存与表 {#内存与表}](#4-内存与表-内存与表)
    - [1 Linear Memory {#linear-memory}](#1-linear-memory-linear-memory)
    - [4 2 Table {#2-table}](#4-2-table-2-table)
  - [5. 导入与导出 {#导入与导出}](#5-导入与导出-导入与导出)
    - [1 Import {#import}](#1-import-import)
    - [5 2 Export {#2-export}](#5-2-export-2-export)
  - [6. 全局变量 {#全局变量}](#6-全局变量-全局变量)
  - [7. 注释与元数据 {#注释与元数据}](#7-注释与元数据-注释与元数据)
    - [1 注释语法 {#注释语法}](#1-注释语法-注释语法)
    - [7 2 Custom Section {#2-custom-section}](#7-2-custom-section-2-custom-section)
  - [8. 名称解析 {#名称解析}](#8-名称解析-名称解析)
    - [1 标识符规则 {#标识符规则}](#1-标识符规则-标识符规则)
    - [8 2 索引与名称混用 {#2-索引与名称混用}](#8-2-索引与名称混用-2-索引与名称混用)
  - [9. 工具链集成 {#工具链集成}](#9-工具链集成-工具链集成)
    - [1 wat2wasm {#wat2wasm}](#1-wat2wasm-wat2wasm)
    - [9 2 wasm2wat {#2-wasm2wat}](#9-2-wasm2wat-2-wasm2wat)
  - [10. 性能考量 {#性能考量}](#10-性能考量-性能考量)
    - [1 解析复杂度 {#解析复杂度}](#1-解析复杂度-解析复杂度)
    - [10 2 生产环境策略 {#2-生产环境策略}](#10-2-生产环境策略-2-生产环境策略)
  - [11. 批判性分析 {#批判性分析}](#11-批判性分析-批判性分析)
    - [1 优势 {#优势}](#1-优势-优势)
    - [11 2 局限性 {#2-局限性}](#11-2-局限性-2-局限性)
    - [11 3 设计反思 {#3-设计反思}](#11-3-设计反思-3-设计反思)
  - [12. 扩展提案 {#扩展提案}](#12-扩展提案-扩展提案)
    - [1 Multi-Memory {#multi-memory}](#1-multi-memory-multi-memory)
    - [12 2 Component Model {#2-component-model}](#12-2-component-model-2-component-model)
  - [15. 参考文献 {#参考文献}](#15-参考文献-参考文献)
  - [13. 相关文档 {#相关文档}](#13-相关文档-相关文档)
  - [关联网络](#关联网络)
    - [前向引用](#前向引用)
    - [后向引用](#后向引用)
    - [交叉链接](#交叉链接)

## 2. 核心命题 {#核心命题}

- _同构性定理_*：
\[
\forall M : \exists W_{\text{WAT}}, B_{\text{binary}} : \text{parse}(W) = \text{decode}(B) = M
\]

- _可读性权衡_*：
\[
\text{Size}(W) \approx 3 \times \text{Size}(B) \quad \text{但 } \text{Debuggability}(W) \gg \text{Debuggability}(B)
\]

- _非规范性_*：

> WAT 是 Wasm 的**语法糖**，不影响语义。工具可自由扩展 WAT，但必须产生规范二进制。

- --

## 1. S-表达式语法 {#s-表达式语法}

### 1 基本结构 {#基本结构}

- _嵌套表达式_*：

```wasm
(module
  (type $t0 (func (param i32) (result i32)))
  (func $f (type $t0)
    local.get 0
    i32.const 42
    i32.add
  )
)
```

- _线性指令序列_*：

```wasm
(func $example
  i32.const 10
  i32.const 20
  i32.add
  drop
)
```

### 1 2 折叠指令（Folded Instructions） {#2-折叠指令folded-instructions}

- _语法糖_*：

```wasm
;; 线性形式
i32.const 1
i32.const 2
i32.add

;; 折叠形式
(i32.add (i32.const 1) (i32.const 2))
```

- _转换规则_*：
\[
(op \; e_1 \; e_2 \; \ldots) \equiv e_1 \; e_2 \; \ldots \; op
\]

- _批判_*：

> 折叠指令提高了代码表达力，但增加了解析器复杂度。S-表达式的嵌套深度可能导致栈溢出，工具链需要递归深度限制。

- --

## 2. 模块声明 {#模块声明}

### 1 Type Section {#type-section}

- _语法_*：

```wasm
(type $sig (func (param i32 i64) (result f32)))
```

- _命名约定_*：

- `$` 前缀标识符：局部作用域
- 可省略名称，使用索引：`(type (func ...))`

### 2 2 Function 声明 {#2-function-声明}

- _完整形式_*：

```wasm
(func $add (param $x i32) (param $y i32) (result i32)
  (local $temp i64)
  local.get $x
  local.get $y
  i32.add
)
```

- _简写形式_*：

```wasm
(func (param i32 i32) (result i32)
  local.get 0
  local.get 1
  i32.add
)
```

- _内联类型_*：

```wasm
(func $inline (param i32) (result i32)
  ;; 等价于自动生成 type 并引用
)
```

- --

## 3. 控制流结构 {#控制流结构}

### 1 Block 与 Label {#block-与-label}

- _语法_*：

```wasm
(block $outer (result i32)
  (block $inner
    br $outer (i32.const 42)
  )
  i32.const 0
)
```

- _标签作用域_*：
\[
\text{Label}(block) := \{ \text{自身}, \text{父级}, \text{祖先} \}
\]

### 3 2 Loop 结构 {#2-loop-结构}

- _循环_*：

```wasm
(loop $continue
  local.get $i
  i32.const 10
  i32.lt_u
  if
    ;; 循环体
    local.get $i
    i32.const 1
    i32.add
    local.set $i
    br $continue  ;; 回到 loop 开头
  end
)
```

- _关键差异_*：

- `block` 的 label 指向 `end`
- `loop` 的 label 指向 `loop` 开头

### 3 3 If-Else {#3-if-else}

- _语法_*：

```wasm
(if (result i32)
  (i32.const 1)
  (then
    i32.const 42
  )
  (else
    i32.const 0
  )
)
```

- _折叠形式_*：

```wasm
(i32.eqz (local.get $flag))
(if
  (then (call $branch_a))
  (else (call $branch_b))
)
```

- --

## 4. 内存与表 {#内存与表}

### 1 Linear Memory {#linear-memory}

- _声明_*：

```wasm
(memory $mem 1 10)  ;; 初始 1 页，最大 10 页
```

- _数据段_*：

```wasm
(data (i32.const 0) "Hello")
(data (memory $mem) (i32.const 1024) "\00\01\02\03")
```

- _主动 vs 被动段_*：

```wasm
;; 主动段：启动时自动初始化
(data (i32.const 0) "active")

;; 被动段：需手动初始化
(data $passive "passive")
(func $init
  i32.const 0      ;; 目标地址
  i32.const 0      ;; 源偏移
  i32.const 7      ;; 长度
  memory.init $passive
)
```

### 4 2 Table {#2-table}

- _声明_*：

```wasm
(table $t 10 20 funcref)
```

- _元素段_*：

```wasm
(elem (i32.const 0) $f1 $f2 $f3)

;; 被动元素
(elem $e funcref (ref.func $f1) (ref.func $f2))
```

- --

## 5. 导入与导出 {#导入与导出}

### 1 Import {#import}

- _语法_*：

```wasm
(import "env" "memory" (memory 1))
(import "wasi_snapshot_preview1" "fd_write"
  (func $fd_write (param i32 i32 i32 i32) (result i32))
)
```

- _两级命名空间_*：
\[
\text{ImportName} := (\text{module}, \text{field})
\]

### 5 2 Export {#2-export}

- _导出声明_*：

```wasm
(func $add (param i32 i32) (result i32)
  local.get 0
  local.get 1
  i32.add
)
(export "add" (func $add))

;; 简写
(func (export "add") (param i32 i32) (result i32)
  local.get 0
  local.get 1
  i32.add
)
```

- --

## 6. 全局变量 {#全局变量}

- _可变全局_*：

```wasm
(global $counter (mut i32) (i32.const 0))

(func $increment
  global.get $counter
  i32.const 1
  i32.add
  global.set $counter
)
```

- _不可变全局_*：

```wasm
(global $pi f32 (f32.const 3.14159))
```

- _初始化约束_*：
\[
\text{GlobalInit} \subseteq \{ \text{const}, \text{global.get imported} \}
\]

- --

## 7. 注释与元数据 {#注释与元数据}

### 1 注释语法 {#注释语法}

- _行注释_*：

```wasm
;; 这是注释
(func $test
  nop  ;; 行尾注释
)
```

- _块注释_*：

```wasm
(;
  多行注释
  可以嵌套 (; 像这样 ;)
;)
```

### 7 2 Custom Section {#2-custom-section}

- _声明_*：

```wasm
(@custom "my-metadata" "arbitrary data")
```

- _工具扩展_*：

```wasm
(@producers
  (language "C" "11.0.0")
  (processed-by "clang" "11.0.0")
)
```

- --

## 8. 名称解析 {#名称解析}

### 1 标识符规则 {#标识符规则}

- _命名空间_*：

- 函数：`$func_name`
- 类型：`$type_name`
- 局部变量：`$local_name`
- 标签：`$label_name`

- _作用域规则_*：

```wasm
(func $outer
  (block $L
    (block $L  ;; 错误：标签冲突
      br $L
    )
  )
)
```

### 8 2 索引与名称混用 {#2-索引与名称混用}

- _等价形式_*：

```wasm
local.get 0
local.get $first_param  ;; 如果 $first_param 是索引 0
```

- _批判_*：

> WAT 允许索引与名称混用，增加了灵活性但降低了可维护性。大型模块中，索引引用容易因插入新定义而失效。

- --

## 9. 工具链集成 {#工具链集成}

### 1 wat2wasm {#wat2wasm}

- _命令行_*：

```bash
wat2wasm module.wat -o module.wasm --debug-names
```

- _选项_*：

- `--debug-names`：保留名称段
- `--no-check`：跳过验证
- `--enable-threads`：启用线程提案

### 9 2 wasm2wat {#2-wasm2wat}

- _反汇编_*：

```bash
wasm2wat module.wasm -o module.wat --fold-exprs
```

- _选项_*：

- `--fold-exprs`：使用折叠指令
- `--inline-exports`：内联导出声明
- `--generate-names`：为未命名项生成名称

- --

## 10. 性能考量 {#性能考量}

### 1 解析复杂度 {#解析复杂度}

- _时间复杂度_*：
\[
T_{\text{parse}}(W) = O(|W| \cdot \text{depth}(W))
\]

- _瓶颈_*：

- S-表达式解析：递归下降
- 名称解析：哈希表查找
- 折叠指令展开：树遍历

### 10 2 生产环境策略 {#2-生产环境策略}

- _最佳实践_*：

1. 仅在开发环境使用 WAT
2. CI/CD 流水线转换为二进制
3. 源码映射用于调试

- --

## 11. 批判性分析 {#批判性分析}

### 1 优势 {#优势}

1. **人类可读**：调试和教学友好
2. **工具友好**：标准 S-表达式易解析
3. **可扩展**：Custom Section 支持元数据

### 11 2 局限性 {#2-局限性}

1. **冗长**：3倍二进制大小
2. **解析慢**：比二进制慢 10-100 倍
3. **语法糖陷阱**：折叠指令掩盖栈行为

### 11 3 设计反思 {#3-设计反思}

> WAT 的 S-表达式设计源于 Lisp 传统，但这导致与主流语法（C/Rust）的认知鸿沟。JavaScript 开发者习惯花括号，看到嵌套括号会感到陌生。也许类 C 语法的文本格式能降低学习门槛，但会牺牲 Lisp 工具链的优势。

- --

## 12. 扩展提案 {#扩展提案}

### 1 Multi-Memory {#multi-memory}

- _语法_*（提案中）：

```wasm
(memory $mem1 1)
(memory $mem2 2)

(func
  i32.const 0
  i32.load (memory $mem2)
)
```

### 12 2 Component Model {#2-component-model}

- _接口定义_*（WIT）：

```wit
interface logger {
  log: func(msg: string)
}

world app {
  import logger
  export run: func()
}
```

- _批判_*：

> Component Model 试图解决 Wasm 的组合性问题，但引入了新的 IDL（WIT），增加了工具链复杂度。是否真的需要另一种接口语言，还是应该直接用 TypeScript/Protocol Buffers？

- --

## 15. 参考文献 {#参考文献}

1. **[WebAssembly Spec]** Text Format (https://webassembly.github.io/spec/core/text/)
2. **[WABT]** WebAssembly Binary Toolkit (https://github.com/WebAssembly/wabt)
3. **[Watt21]** Conrad Watt. "Mechanising and Verifying the WebAssembly Specification." CPP, 2021.

- --

## 13. 相关文档 {#相关文档}

- **[02.1 二进制编码](02.1_Binary_Encoding.md)** - WAT 到二进制的映射
- **[02.3 模块结构](02.3_Module_Structure.md)** - 模块组织原则
- **[03.1 编译策略](../03_Runtime_Systems/03.1_Compilation_Strategies.md)** - WAT 解析器实现


---

## 关联网络

### 前向引用

> 本文档为以下文档提供基础：
>
> - [相关文档](./) (待补充)

### 后向引用

> 本文档依赖以下基础文档：
>
> - [基础文档](./) (待补充)

### 交叉链接

> 相关主题：
>
> - [主题A](./) (待补充)
> - [主题B](./) (待补充)

---

_本文档由 FormalScience 文档规范化工具自动生成_
